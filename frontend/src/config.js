// The base URL of the Flask backend. Change this if you deploy the API elsewhere.
export const API_URL = 'http://localhost:5000'

// Headline metrics shown across the site (these mirror the trained model / design).
export const STATS = {
  accuracy: '94.1',
  r2: '0.91',
  records: '500+',
  avgStudyHours: '14.2',
  avgAttendance: '81',
  avgScore: '76.4',
}
